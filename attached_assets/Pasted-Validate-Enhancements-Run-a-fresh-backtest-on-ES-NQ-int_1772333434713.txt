Validate Enhancements: Run a fresh backtest on ES/NQ (intraday 5-min or 15-min, stitched contracts 2020–2026) to compare pre/post enhancements. Expect:
Fewer but higher-quality trades (sideways filter eliminates chop).
Higher average confluence score on winners (e.g., igniting vol + bottoming tail + strong body).
Vol Type column helping classify setups (prefer igniting on breakout/3BP trigger, resting in handle/consolidation).
If win rate/PF improves → lock in; if too few trades → relax sideways threshold slightly (e.g., 0.3% EMA convergence) or add ADX >20 filter for trending confirmation.

Add Multi-Timeframe Alignment (MTF): Core from manual Ch.5 (trends) and Live Traders emphasis on higher TF bias. Implement as filter:
Fetch daily/60-min data → check uptrend (higher highs/lows or price > EMA21 daily).
Require long signals only if higher TF uptrend; shorts in downtrend.
Confluence +1 if MTF aligned.
Code snippet (paste into backtester):

Python# Fetch higher TF (e.g., daily)
daily_aggs = client.get_aggs('ESZ5', 1, 'day', start_date, end_date)  # Adjust contract/date
daily_df = pd.DataFrame(daily_aggs)[['timestamp', 'close']].set_index('timestamp')
daily_df['ema21_daily'] = daily_df['close'].ewm(span=21).mean()
daily_df['htf_up'] = daily_df['close'] > daily_df['ema21_daily']

# Resample lower TF signals to check latest daily state
data['date'] = data.index.date
data = data.merge(daily_df[['htf_up']], left_on='date', right_index=True, how='left')
data['htf_up'] = data['htf_up'].ffill()  # Forward fill daily state

# Filter
data['signal_long'] = data['signal_long'] & data['htf_up']
# Add to confluence: +1 if htf_up

Enhance Confluence Scoring Further: Current 0–5 good; add MTF (+1), pattern booster (tail on key bar +1), gap presence (+0.5–1 if Level 3 gap). Aim for signals ≥4 only in live scanner.
Next Recommendations:
Intraday Shift + Symbol Expansion: Move backtester to 5-min/15-min ES/NQ. Current daily/low frequency is expected; intraday captures real Live Traders setups (e.g., 3 Bar Play on 5-min after daily bias). Test NQ parallel.
Add Retest Buy/Sell Setup: From research, Live Traders heavy emphasis (double bottom retest, retest after pullback). Detect as: pullback to prior high/low + bottoming tail + vol support + MA respect. High confluence with 3 Bar Play trigger.
Journal & Optimization Loop: Feed backtest trades to Supabase journal → compute edge per pattern/vol type/confluence level. Use A/B: threshold 4 vs 5 confluence.
Psychology/Edge Reminders: Add manual-inspired notes in Edge Builder UI (e.g., "Trade only what you can afford to lose", "Bias bias bias", "Process over outcome").
4 Bar Play Variant: Extend 3 Bar Play logic to allow 2 resting bars (4 total) for stronger setups.

Visuals — Examples of key manual/Live Traders elements: bottoming tail confluence on 3 Bar Play, resting volume pause, strong trend with MA respect.